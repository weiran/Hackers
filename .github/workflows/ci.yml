name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Lint
      uses: maierj/fastlane-action@v1.4.0
      with: 
        lane: 'lint'
    - name: Run UI Tests
      uses: maierj/fastlane-action@v1.4.0
      with: 
        lane: 'test'
    - name: Build
      uses: maierj/fastlane-action@v1.4.0
      with: 
        lane: 'build'

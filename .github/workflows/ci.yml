name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  lint:
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v3.1
    - uses: ruby/setup-ruby@v1.0
      with:
        ruby-version: '2.7.3'
        bundler-cache: true
    - name: Run SwiftLint
      uses: maierj/fastlane-action@v3
      with: 
        lane: 'lint'
        subdirectory: 'fastlane'

  test:
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v3.1
    - uses: ruby/setup-ruby@v1.0
      with:
        ruby-version: '2.7.3'
        bundler-cache: true
    - uses: AckeeCZ/load-xcode-version@1
    - name: Run UI Tests
      uses: maierj/fastlane-action@v3
      with: 
        lane: 'test'
        subdirectory: 'fastlane'

  build:
    runs-on: macos-15

    steps:
    - uses: actions/checkout@v3.1
    - uses: ruby/setup-ruby@v1.0
      with:
        ruby-version: '2.7.3'
        bundler-cache: true
    - uses: AckeeCZ/load-xcode-version@1
    - name: Build
      uses: maierj/fastlane-action@v3
      with: 
        lane: 'build'
        subdirectory: 'fastlane'

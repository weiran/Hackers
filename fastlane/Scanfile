# For more information about this configuration visit
# https://docs.fastlane.tools/actions/scan/#scanfile

scheme("HackersUITests")
devices(["iPhone 13 Pro"])
# devices(["iPhone 13 Pro", "iPad Pro (11-inch) (3rd generation)"])

# Enable skip_build to skip debug builds for faster test performance
skip_build(true)

# Do not run test bundles in parallel on the specified destinations. Testing will occur on each destination serially
disable_concurrent_testing(true)